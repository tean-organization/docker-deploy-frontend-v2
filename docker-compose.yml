services:
  frontend:
    image: teanchai1112/234-deploylab-frontend:latest
    pull_policy: always
    ports:
      - "8081:8080"
    environment:
      - VITE_GRAPHQL_URI=http://<IP_VM>:8082/graphql
      - VITE_SERVER_URI=http://<IP_VM>:8082
    restart: unless-stopped